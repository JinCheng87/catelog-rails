<%= form_for(manufacturer) do |f| %>
  <% if manufacturer.errors.any? %>
    <div>
      <h2><%= pluralize(manufacturer.errors.count,'error')%> prohibited this manufacturer frombeing saved:</h2>

      <ul>
        <% manufacturer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <label>Name</label>
    <input type="text" name="manufacturer[name]" value="<%= manufacturer.name %>">
  </div>

  <div>
    <label>Description</label>
    <input type="text" name="manufacturer[description]" value="<%= manufacturer.description %>">
  </div> 

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>